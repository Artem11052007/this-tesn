#include <iostream>
#include <iomanip>
using namespace std;
int main()
{ const int n=10;
  int a[n] = {5, 8, 77, 10, 8};
  srand(time(NULL));
  for(int i =0; i<n; i++)
  //cin >> a[i]; 
  a[i] = rand()%20 -10;
  for(int i =0; i<n; i++)
 cout << setw(5) << a[i];
 int cum = 0;
    for(int i =0; i<n; i++) {
     if (a[i]>5) {
    cum += a[i];

        cin >> number;
    }

    int h = number / 100, t = (number / 10) % 10, u = number % 10;
    cout << (h < t && t < u ? "Цифри утворюють зростаючу послідовність" :
               h > t && t > u ? "Цифри утворюють спадну послідовність" :
               "Цифри не утворюють зростаючу або спадаючу послідовність") << endl; h > t && t > u ? "Цифри утворюють спадну послідовність" :
               "Цифри не утворюють зростаючу або спадаючу послідовність") << endl; h > t && t > u ? "Цифри утворюють спадну послідовність" :
               "Цифри не утворюють зростаючу або спадаючу послідовність") << endl; h > t && t > u ? "Цифри утворюють спадну послідовність" :
               "Цифри не утворюють зростаючу або спадаючу послідовність") << endl; h > t && t > u ? "Цифри утворюють спадну послідовність" :
               "Цифри не утворюють зростаючу або спадаючу послідовність") << endl; h > t && t > u ? "Цифри утворюють спадну послідовність" :
               "Цифри не утворюють зростаючу або спадаючу послідовність") << endl; h > t && t > u ? "Цифри утворюють спадну послідовність" :
               "Цифри не утворюють зростаючу або спадаючу послідовність") << endl;

    return 0;
}
